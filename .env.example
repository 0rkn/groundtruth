CF_ACCOUNT_ID=
CF_API_TOKEN=
CF_VECTORIZE_INDEX=groundtruth-index
CF_KV_NAMESPACE_ID=
# Passcode gate. Required in production — see README's "Access control" and HANDOVER.md's
# "Before deploying". Fails closed (503) if unset, never open.
APP_PASSCODE=
